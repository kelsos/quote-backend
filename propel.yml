propel:
    database:
        connections:
            default:
                adapter: mysql
                dsn: mysql:host=localhost;dbname=quote
                user: homestead
                password: secret
                settings:
                    charset: utf8
